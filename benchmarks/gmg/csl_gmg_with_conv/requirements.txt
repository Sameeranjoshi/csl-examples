# Python dependencies for the GMG V-cycle solver (host-side + plotting).
# Versions pinned to match the Cerebras SDK 1.4.0 / release 2.5.0 environment
# the artifact was developed and evaluated against.
#
# Cerebras SDK packages (installed by the Cerebras SDK distribution; listed
# here for completeness — do NOT pip-install from PyPI, use the SDK's req.txt):
#   cerebras-appliance==2.5.0
#   cerebras-sdk==2.5.0

numpy==1.24.4
matplotlib==3.7.5
scipy==1.10.1
pandas==2.0.3
numba==0.58.1   # optional; speeds up the host reference solver (python_gmg/gmgoscar.py)
