comptime {
  @comptime_assert(@is_arch("wse1"));
}

const valid_task_id_types = [3]type {
  color,
  data_task_id,
  local_task_id,
};

const valid_task_id_error_message =
  "Expected task_id to have type 'color', 'data_task_id', or 'local_task_id'";
